// Datei: ./werkzeuge/paketoperationen/pakete-ueber-den-namen-finden/ubuntuprojekt.adoc

// Baustelle: Rohtext

===== Suche über die Webseite des Ubuntu-Projekts =====

// Stichworte für den Index
(((Paketsuche, über den Paketnamen)))
(((Paketsuche, über die Paketbeschreibung)))
(((Paketsuche, über den Paketinhalt)))
(((Paketsuche, über packages.ubuntu.org)))
(((Paketsuche, anhand der Veröffentlichung)))
(((Paketsuche, anhand des Quellpakets)))

* http://packages.ubuntu.com/
* Suche
** Vorauswahl anhand der Distribution (oberer Teil der Webseite)
** anhand eines Begriffs bezogen auf den Paketnamen, die
   Paketbeschreibung oder den Namen des Quellpakets
** Eingrenzung auf die Veröffentlichung und den Distributionsbereich
   (hier genannt ``Section'')

// Abbildung von packages.ubuntu.org
.Ergebnis der Paketsuche nach 'aptsh' über http://packages.ubuntu.org/
image::werkzeuge/paketoperationen/pakete-ueber-den-namen-finden/paketsuche-ubuntu-webseite.png[id="fig.packages-ubuntu-Webbrowser", width="50%"]

// Datei (Ende): ./werkzeuge/paketoperationen/pakete-ueber-den-namen-finden/ubuntuprojekt.adoc
